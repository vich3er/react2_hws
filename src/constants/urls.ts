const base_url_json = "https://jsonplaceholder.typicode.com"
const base_url_dummy = "https://dummyjson.com"

export const getUrlJson = {
    users: base_url_json + "/users",
    posts: base_url_json + "/posts",
    comments: base_url_json + "/comments",
}

export const getUrlDummy = {
    users: base_url_dummy + "/users",
    posts: base_url_dummy + "/posts",
    comments: base_url_dummy + "/comments",
}